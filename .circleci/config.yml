version: 2.1
orbs:
  node: cypress-io/cypress@1.29.0
jobs:
  test:
    executor: cypress/default
    steps:
      - checkout
      - run:
          name: Install dependencies
          command: npm install
      - run:
          name: Run Cypress Tests
          command: npm test
workflows:
  test_app:
    jobs:
      - test