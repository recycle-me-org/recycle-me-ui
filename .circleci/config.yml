version: 2.1
jobs:
  test:
    docker:
      - image: node/alpine
    steps:
      - checkout
      - run:
          name: Install dependencies
          command: npm install
      - run:
          name: Run Cypress Tests
          command: npm test
workflows:
  test_app:
    jobs:
      - test